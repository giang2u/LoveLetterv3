{# src/OC/PlatformBundle/Resources/views/Advert/cartes.html.twig #}

<!DOCTYPE html>
<html>
  <head>
    <title>Bienvenue sur LoveLetter !</title>
  </head>
  <body>
    <h1>Liste des carte!</h1>
		<img src="{{ asset(Guard) }}" alt="Guard" />
		<img src="{{ asset(Priest) }}" alt="Priest" />
		<img src="{{ asset(Baron) }}" alt="Baron" />
		<img src="{{ asset(Handmaid) }}" alt="Handmaid" />
		<img src="{{ asset(Prince) }}" alt="Prince" />
		<img src="{{ asset(King) }}" alt="king" />
		<img src="{{ asset(Countess) }}" alt="Countess" />
		<img src="{{ asset(Princess) }}" alt="Princess" />
  </body>
</html>
